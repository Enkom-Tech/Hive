# Hive infra

Operator, worker, manifests, GitOps, and scripts for Hive worker orchestration. Run `make` from this directory, or from repo root use `make -C infra <target>`.

See the [repo root README](../README.md) for quick start, WSL2 setup, and layout. Key targets: `make ci`, `make e2e`, `make crds`.
