# DocIndex parser worker — heavy image (Docling + Unstructured).
# Lock: pip-compile worker/requirements.txt -o worker/requirements.lock
# After Dependabot bumps this file, regenerate the lock from infra/docindex-lancedb/.
#
# CPU-only PyTorch index keeps locks and Docker images free of CUDA/NVIDIA transitive deps.
--extra-index-url https://download.pytorch.org/whl/cpu

httpx>=0.28.1,<0.29.0
redis>=7.4.0,<8.0
pydantic>=2.13.3,<3.0
pydantic-settings>=2.14.0,<3.0
structlog>=25.5.0,<26.0
docling>=2.91.0,<3.0
unstructured>=0.18.32,<0.19.0
