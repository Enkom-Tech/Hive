name: Control plane E2E

on:
  workflow_dispatch:
    inputs:
      skip_llm:
        description: "Skip LLM-dependent assertions (default: true)"
        type: boolean
        default: true
  push:
    branches: [main]
    paths: ['control-plane/**']
  schedule:
    - cron: '0 2 * * *'

concurrency:
  group: control-plane-e2e-${{ github.event_name }}-${{ github.sha || github.run_id }}
  cancel-in-progress: false

jobs:
  e2e:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    defaults:
      run:
        working-directory: control-plane
    env:
      HIVE_E2E_SKIP_LLM: ${{ github.event_name == 'workflow_dispatch' && (inputs.skip_llm && 'true' || 'false') || 'true' }}

    steps:
      - uses: actions/checkout@v6

      - uses: pnpm/action-setup@v5
        with:
          version: 9

      - uses: actions/setup-node@v6
        with:
          node-version: 20
          cache: pnpm
          cache-dependency-path: control-plane/pnpm-lock.yaml

      - run: pnpm install --frozen-lockfile
      - run: pnpm build

      - uses: actions/setup-go@v6
        with:
          go-version-file: infra/worker/go.mod
          cache-dependency-path: infra/worker/go.sum

      - name: Build hive-worker (MCP E2E smoke)
        working-directory: infra/worker
        run: go build -o hive-worker .

      - run: npx playwright install --with-deps chromium

      - name: Run e2e tests
        if: github.event_name != 'workflow_dispatch' || inputs.skip_llm
        env:
          HIVE_E2E_HIVE_WORKER_BINARY: ${{ github.workspace }}/infra/worker/hive-worker
          HIVE_WORKER_JWT_SECRET: hive-e2e-worker-jwt-secret-at-least-32-chars!!
          HIVE_E2E_MCP_MATERIALIZE_SECRET: hive-e2e-mcp-materialize-secret
        run: pnpm run test:e2e

      - name: Run e2e tests (with LLM)
        if: github.event_name == 'workflow_dispatch' && !inputs.skip_llm
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          HIVE_E2E_HIVE_WORKER_BINARY: ${{ github.workspace }}/infra/worker/hive-worker
          HIVE_WORKER_JWT_SECRET: hive-e2e-worker-jwt-secret-at-least-32-chars!!
          HIVE_E2E_MCP_MATERIALIZE_SECRET: hive-e2e-mcp-materialize-secret
        run: pnpm run test:e2e

      - uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f # v7.0.0
        if: always()
        with:
          name: playwright-report
          path: |
            control-plane/tests/e2e/playwright-report/
            control-plane/tests/e2e/test-results/
          retention-days: 14
